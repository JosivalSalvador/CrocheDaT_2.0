# Dependências
node_modules
**/node_modules

# Build outputs (exceto o que o Docker precisa gerar)
dist
coverage
.turbo
.tsbuildinfo
**/*.tsbuildinfo

# Next.js (gerado no build, não entra no contexto)
.next
**/.next

# Env / secrets
.env
.env.*
!.env.example

# VCS / IDE
.git
.github
.vscode
.idea

# Logs
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Docs / Docker
README.md
docker-compose.yml
docker-compose.dev.yml
